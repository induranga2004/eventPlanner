# API server configuration
PORT=4000
NODE_ENV=development

# MongoDB Connection (required for auth/subscription persistence)
MONGO_URI=mongodb://localhost:27017/event-planner

# Authentication secrets
# Generate strong secrets here: https://www.grc.com/passwords.htm
JWT_SECRET=your-super-secret-jwt-key
TOTP_SECRET_KEY=totp-dev-seed

# Frontend origin for Stripe redirects & CORS helpers
FRONTEND_URL=http://localhost:5173

# Optional: Stripe subscription setup (leave unset in local dev)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
MANUAL_UPGRADE_TOKEN=dev-service-token
