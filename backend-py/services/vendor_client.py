"""Deprecated vendor client module.

The planner service no longer fetches vendor catalogs or recommends
providers, so this module is intentionally left empty. It remains in
place to avoid import errors for any legacy code that might still
reference it.
"""


__all__: list[str] = []
